# dependencies / build output
node_modules/
dist/
src-tauri/target/
src-tauri/gen/
__pycache__/
*.py[cod]
.venv/
venv/

# secrets — UW_API_KEY, ANTHROPIC_API_KEY live here, never in git
.env
.env.*
!.env.example

# PyInstaller output. engine.spec IS source and stays tracked.
engine/build/
engine/dist/

# local data — includes engine/data_store/oi/, the daily OI snapshots.
# Those are irreplaceable (OI history can't be fetched retroactively) but they
# are yours, not the repo's. Back them up separately if you care about them.
data_store/
*.csv
*.log

# Generated journal notes. The vault is an OUTPUT of the app, not source, and
# a verification run used to leave a dated note here before the suite
# sandboxed it.
engine/obsidian_out/
